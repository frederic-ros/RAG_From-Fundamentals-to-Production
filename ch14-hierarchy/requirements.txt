# ch14-hierarchy — dependencies
scikit-learn>=1.3.0    # recherche TF-IDF sur les fragments-enfants (tous les TP)

# tiktoken gives the real OpenAI token count (optional).
#   tiktoken>=0.5.0
#   If it is absent or cannot download its encoding, the tokenizer.py module
#   falls back automatically to a deterministic approximate count (words + punctuation),
#   enough for the labs and fully offline.
#
# No API key is required to run the labs.
