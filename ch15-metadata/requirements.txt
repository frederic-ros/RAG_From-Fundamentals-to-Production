# ch15-metadata — dependencies
scikit-learn>=1.3.0    # recherche TF-IDF (socle de repli, tous les TP)

# sentence-transformers gives REAL dense embeddings (recommended at this point
# in the book, to get closer to real conditions). Optional:
#   sentence-transformers>=2.2.0
#   On first load, the paraphrase-multilingual-MiniLM-L12-v2 model
#   is downloaded. If it is absent and there is no network, the module
#   embeddings.py falls back automatically to the TF-IDF base (scikit-learn),
#   deterministic and offline. The chapter's demonstrations hold in
#   both cases.
#
# No API key is required to run the labs.
