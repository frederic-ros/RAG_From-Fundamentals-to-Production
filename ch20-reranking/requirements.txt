# ch20-reranking — dependencies

numpy>=1.24.0          # vectors and computations (MMR, metrics)
scikit-learn>=1.3.0   # TF-IDF du bi-encoder en mode hors-ligne (repli)

# sentence-transformers gives REAL bi- and cross-encoders (optional):
#   sentence-transformers>=2.2.0
#   - bi-encoder  : all-MiniLM-L6-v2
#   - cross-encoder : cross-encoder/ms-marco-MiniLM-L-6-v2
#   If it is absent or cannot download its models, reranklib.py bascule
#   automatically to a deterministic fallback (TF-IDF + overlap score),
#   which reproduces the chapter's lessons faithfully, fully offline.
#
# No API key is required to run the labs.
