# ch09-tables — dependencies
pandas>=2.0.0
openpyxl>=3.1.0          # reading and writing Excel
scikit-learn>=1.3.0      # TF-IDF (a lightweight vector search)

# sqlite3 is in the Python standard library (Lab 9-4, Lab 9-6).
#
# Lab 9-1 offers three engines ; the in-house engine (scikit-learn) is enough.
# The other two are OPTIONAL and detected automatically:
#   faiss-cpu>=1.7.0      # FAISS engine (optional)
#   chromadb>=0.4.0       # ChromaDB engine (optional)
#
# Lab 9-4 et Lab 9-5 translate questions into SQL by rules (OFFLINE mode) with nothing
# de plus. Optional LOCAL mode: installer Ollama puis `ollama pull llama3.2`.
#
# No API key is required to run the labs.
