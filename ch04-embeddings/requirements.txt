# ch04-embeddings — dependencies
numpy>=1.24.0          # vectors and cosine similarity (every lab)
scikit-learn>=1.3.0    # TF-IDF baseline and PCA projection (Lab 4-1, 4-3)
matplotlib>=3.7.0      # 2-D map of meaning (Lab 4-3 only)

# sentence-transformers gives real embeddings instead of the pedagogical ones
# (optional).
#   sentence-transformers>=2.2.0
#   Without it the labs fall back to a deterministic TF-IDF representation,
#   which shows the same geometry with a less spectacular semantic effect.
#
# No API key is required to run the labs.
