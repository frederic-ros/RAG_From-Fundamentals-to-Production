# ch21-query-transformation — dependencies

numpy>=1.24.0          # vectors and metrics
scikit-learn>=1.3.0   # TF-IDF du retrieval en mode hors-ligne (repli)

# sentence-transformers gives a REAL dense embedding engine (optional):
#   sentence-transformers>=2.2.0   (model all-MiniLM-L6-v2)
#   If it is absent or cannot download its model, qtlib.py bascule
#   automatically to deterministic TF-IDF retrieval.
#
# IMPORTANT — the transformations (HyDE, expansion, decomposition, step-back,
# rewriting) are SIMULATED deterministically (rules + vocabulary templates),
# instead of an LLM call. No API key and no local LLM are required:
# the labs stay fast, reproducible and offline. The teaching point of
# each transformation appears clearly in this mode.
